__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/

# Runtime experiment outputs (regenerable via `python -m aethergrid.run` / `aethergrid.evaluate`)
aethergrid/runs/

# Streamlit runtime cache
.streamlit/secrets.toml

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code internal scratch (plan-mode agent worktrees)
.claude/worktrees/

# Data caches (raw/processed are populated at runtime if ever used; synthetic/ is generated in-process, not on disk)
aethergrid/data/raw/*
aethergrid/data/processed/*
!aethergrid/data/raw/.gitkeep
!aethergrid/data/processed/.gitkeep
